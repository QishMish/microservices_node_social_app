apiVersion: v1
kind: PersistentVolume # Create PV 
metadata:
  name: user-postgres-volume # Sets PV name
  labels:
    type: local # Sets PV's type
    app: user-postgres
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi # Sets PV's size
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/data/postgresql" # Sets PV's host path